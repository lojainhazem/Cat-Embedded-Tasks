#include <stdio.h>

int main() {
    int num;
    
    printf("Enter a number: ");
    scanf("%d", &num);

   
    if (num%2==0) {
        printf("this num is even");
    }
    else{
        printf("this num is odd");
    }

    return 0;
}